* Create an ODT but also create a PDF.
* Add the name of the parasha and the date to the header of the generated document.
* Create a helper script for someone on MacOS to run that will let them run this script. they might not have python installed or know how to install it so you'd have to do it for them with the venv and everything.